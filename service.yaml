apiVersion: v1
kind: Service
metadata:
  name: php-app-service
spec:
  type: NodePort
  selector:
    app: php-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080 # Ce port sera utilisé pour voir l'app sur AWS